# Read-only preflight and baseline

Assessment date: 2026-09-17. This record contains only tool versions, source revisions, public API metadata, availability booleans and aggregate census state. No credentials, project names/IDs, account identity, application logs or end-user data were printed or saved.

## Verified environment

| Check | Actual result |
| --- | --- |
| Alchemy starting HEAD | `25edaebe4e99debea6a972cca69d124188763457` |
| Distilled starting HEAD | `c2a7800210ac2fc9aec62e905f09d7f725b782e7` |
| Existing work | Distilled submodule already dirty; left untouched. No existing `processes/Neon/catalog/`. Only Project/Branch registered in Neon Providers. The SDK owner subsequently changed generated source during catalog writing; SDK gaps here describe the initial snapshot, not a claim that concurrent fixes are absent. |
| Node | `v24.10.0`; satisfies Node 24 major. Workspace memory reports a node-utils failure on this patch version, fixed on 24.21.0; that regression was not rerun here. |
| pnpm | `11.25.0` |
| Bun | `1.3.13` |
| Hard timeout | `/opt/homebrew/bin/timeout` available |
| `NEON_API_KEY` | Present; final established-layer resolution and getAuthDetails/listProjects succeeded. No value or account identity printed; mutation permission not tested. |
| Root dependencies | Initially unresolved. A later read-only recheck after concurrent coordinator work succeeds: Alchemy resolves Neon source; root/Alchemy/Neon/core resolve the same realpath for Effect 4.0.0-rc.115. This agent performed no installation. |
| Credential layer | Initial bounded import failed with sanitized `dependencies_not_resolvable`. Final probe successfully used `Neon.Credentials.fromAuthProvider()` with NeonAuth, profile/credential store layers, NodeServices, registry, suppressed credential-source logging and FetchHttpClient. |
| Testing profile | Command selected `ALCHEMY_PROFILE=testing`, but available environment key took precedence. Stored testing-profile existence/configuration remains unverified; credential/profile files were never read-print inspected. |
| Authenticated Neon requests | **4 read-only SDK requests**, automatic retries disabled. getAuthDetails succeeded; getCurrentUserInfo and getActiveRegions returned typed NotFound; listProjects succeeded with an empty first page. No raw-token HTTP fallback. |
| Cloud mutations | **0**. No deploy/destroy/reveal/rotate/credit purchase/DNS changes. |
| DNS authority / AI entitlement | Unverified; no DNS account lookup or model invocation performed. |

Environment credentials take precedence over a selected profile under `Auth/Resolve.ts`. Thus `--profile testing` alone is not proof that mutations target a testing account when `NEON_API_KEY` is present. Coordinator must confirm the resolved account is authorized before mutation without logging its identity here.

## Prisma merge verification

Read-only `gh pr view 1683 --repo alchemy-run/alchemy --json state,mergedAt,headRefOid,mergeCommit` returned:

- State: `MERGED`.
- Head: `1503d1777d22a2a0e4dccbc3d59ae11c507412fa` (same approved reference).
- Merged at `2026-09-17T21:39:07Z`.
- Merge commit: `530a201e497a0ddba9e7be12ed0f0180cc8b668c`.

The reference commit object is available locally; its Website files were read with `git show`. The starting HEAD does not include the Prisma Website directory. No main merge, cherry-pick, checkout, commit or PR was performed. This supersedes the plan and memory's older open/unmerged status.

## Public API/source evidence

A system-TLS `curl --max-time 30` under `timeout 240` successfully read `https://neon.com/api_spec/release/v2.json` without authorization. It is OpenAPI 3.0.3 with 179 HTTP operations at inspection; checked-in SDK source had 158 operation exports. The count difference is not itself a mandate to expose every operation as IaC.

Public trigger CRUD operation IDs are `listProjectBranchTriggers`, `createProjectBranchTrigger`, `getProjectBranchTrigger`, `updateProjectBranchTrigger`, and `deleteProjectBranchTrigger`; absent from the assessed SDK. Public bucket operations still contain no PATCH for visibility. Trigger request/response `oneOf` variants and nullable schedule `next_run_at` were inspected. No public spec file was persisted or regenerated by this agent.

An earlier Python urllib attempt failed with `URLError`; curl with normal certificate verification succeeded. This was a local fetch-path issue, not a Neon account/API entitlement failure. Public docs were also read for S3 compatibility, triggers, injected environment and runtime limits. `/docs/storage/regions` returned a public documentation not-found page; it is not account-region evidence. Ohio support remains a plan/documentation assumption to confirm against actual service availability.

## Final read-only probe and aggregate baseline

The probe ran from packages/alchemy under `ALCHEMY_PROFILE=testing timeout 240 bun --no-install -e ...`, with the established credential layers, no automatic SDK retries, ten-second per-operation timeouts and a 100-second overall Effect deadline. Project enumeration allowed at most ten pages but terminated on its first empty page. Output contained only these safe results:

| Probe | Actual result |
| --- | --- |
| Credential resolution | resolved; environment precedence; expected default Neon API origin |
| getAuthDetails | success (identity response deliberately discarded) |
| getCurrentUserInfo | typed `NotFound`; no raw body/message logged |
| getActiveRegions | typed `NotFound`; no raw body/message logged |
| listProjects | success; 0 accessible projects, 0 proposed-namespace matches, 1 page, complete for this token/query |

The NotFound causes are **not diagnosed**: they are not proof of invalid credentials, no available regions, lack of feature entitlement or a Neon outage. SDK owner/coordinator should check the operation/path/account contract. No unsafe status duck-typing, repeated speculative probes or account-token HTTP workaround was used.

| Aggregate | Value | Scope/limitation |
| --- | --- | --- |
| Accessible projects | **0** | Generated listProjects, default scope/no org filter, one complete empty page for resolved environment credential |
| Proposed own-namespace name matches | **0** | Prefix alchemy-neon-compute-backend-; not ownership authorization |
| Authorized own-namespace projects | none selected | No project exists in returned collection; explicit creation/ownership remains implementation work |
| Active regions / Ohio presence | unknown | getActiveRegions returned typed NotFound |
| Branches/endpoints/backend child resources | not enumerated | No authorized owned project selected; do not infer account-global zero from an empty scoped project list |

This establishes a limited **visible-project baseline of zero**, not an account-wide leak census or permission to mutate arbitrary organizations/shared projects. Region availability, creation rights and a concrete owned test namespace remain unverified. Other agents may create projects after this point; re-census before mutation rather than assume this snapshot is still empty.

## Recommended scope-isolated baseline continuation

1. Root dependency resolution and single Effect identity now pass. Keep dependency management with the coordinator, never a standalone SDK install. Do not run tsc/build as an agent.
2. Use the existing `Neon.Credentials.fromAuthProvider()` with `NeonAuth`, `ProfileStoreLive`, `CredentialsStoreLive`, platform services and `FetchHttpClient.layer` (composition modeled by Neon Providers). Supply `ALCHEMY_PROFILE=testing` through established config selection and suppress normal credential-source diagnostics. Resolve only availability/source booleans; never unwrap or print the account key.
3. Under `timeout 240`, use generated `getAuthDetails({})` and, if its authorization permits, `getCurrentUserInfo({})` only to establish account availability. Project/organization-scoped tokens may not allow user-account endpoints; retain the exact typed authorization result as that probe's blocker, not a claim that project CRUD is unavailable. Do not log account response bodies. Use `getActiveRegions({ org_id? })` and report region count plus Ohio-present boolean. This endpoint does not itself prove Functions/storage/AI entitlement.
4. Page generated `listProjects({ limit: 100, cursor, org_id? })` with at most ten pages and a sub-60-second overall budget. Stop on empty rows, missing or repeated cursor; mark incomplete on page/budget exhaustion. Keep only aggregate counts and an in-memory set of identities needed for authorized scope selection. Do not call existing `ProjectProvider.list`/`BranchProvider.list`: these hydrate connection credentials across the account and exceed a minimal census.
5. Reserve a deterministic stage such as `ALCHEMY_TEST_STAGE=neon_compute_backend` and an own project namespace such as `alchemy-neon-compute-backend-<owner>`; include stack/logical ID/instance suffixes through PhysicalName. Coordinate the owner suffix rather than choosing a different name on retries. A matching name is not sufficient ownership: verify stack state/internal ownership before touching an existing project. Prefer one explicitly owned parent project with bounded per-owner child branches for backend suites; isolate Project replacement tests into their own owned projects. Do not use arbitrary existing projects or assume account-wide absence.
6. Before any mutation, count resources only within those verified project/branch identities using `listProjectBranches`, `listProjectBranchEndpoints`, `listProjectBranchBuckets`, `listProjectBranchFunctions`, `listProjectBranchCustomDomains`, `listCredentials`, `getNeonAuth`, and `getProjectBranchDataAPI`. After SDK regeneration use `listProjectBranchTriggers`. For an owned test bucket only, aggregate `listObjectsV2` and `listMultipartUploads` without logging keys. Do not query end-user Auth accounts or application object/SQL contents for a census. Record own/inherited counts separately.
7. Keep parent baseline configuration and credentials out of logs; use aggregate before/after counts and state-backed own IDs privately during tests. Reuse no borrowed parent as a teardown target. Start/end each lifecycle with normal stack destroy. Preserve observed unexpected residue as forensic evidence for its provider owner instead of adding test-side deletion workarounds.
8. Run 4–6 implementation owners at most, with distinct owned child branches and coordination around parent lifecycle. After every scoped round check no own-resource delta remains; inherited parent resources remain unchanged. Require two consecutive complete no-leak rounds. No account-wide nuke or broad cleanup, including while peers are active.

Outstanding blockers: getCurrentUserInfo/getActiveRegions typed NotFound needs contract/account diagnosis; authorized creation and service/region eligibility; a concrete owned project/branch baseline; all mutation/runtime acceptance; DNS authority and actual HTTPS; AI entitlement/credits; SDK owner's final validation of initial trigger/binary/security gaps. Dependency resolution and credential/auth/project-list probes now pass, but no lifecycle/runtime acceptance is implied.

## Catalog handoff

`git check-ignore -v processes/Neon/catalog/preflight.md` identifies root `.gitignore` line 77 `/processes/`. These explicitly requested files therefore exist on disk but are ignored by ordinary git status. This agent did not modify ignore rules, stage files, commit or create PRs. If the coordinator wants them tracked, it must explicitly include the scoped catalog paths. Concurrent resource/SDK/framework edits belong to other owners and were left untouched; initial inventory statuses are intentionally not promoted from their unreviewed work.

Catalog validation passed: 26 files (25 Markdown plus summary.json), 38 unique required items, 13 individual Website specs, 2 initial partial/36 initial missing, valid JSON/relative links, and no whitespace errors (`git diff --no-index --check` for ignored files). All 74 cataloged management operation names are now present in the SDK owner's concurrently regenerated source. This is name/existence validation only, not wire/schema/live correctness or permission to mark the initial SDK gaps resolved. No builds/typechecks or lifecycle tests ran.
