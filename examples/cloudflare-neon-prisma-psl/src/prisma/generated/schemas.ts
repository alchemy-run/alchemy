// Generated by alchemy prisma generate.
import * as Schema from "effect/Schema";

export const schemas = {
  public: {
    Post: Schema.Struct({
      authorId: Schema.String.check(Schema.isGUID()),
      id: Schema.String.check(Schema.isGUID()),
      title: Schema.String,
    }),
    User: Schema.Struct({
      createdAt: Schema.String,
      email: Schema.String,
      id: Schema.String.check(Schema.isGUID()),
      name: Schema.String,
    }),
  },
} as const;
