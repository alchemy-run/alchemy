datasource db {
  provider = "sqlite"
}

generator client {
  provider = "prisma-client"
  output   = "./generated"
  runtime  = "workerd"
}

model Widget {
  id        Int      @id @default(autoincrement())
  name      String
  createdAt DateTime @default(now())
}
