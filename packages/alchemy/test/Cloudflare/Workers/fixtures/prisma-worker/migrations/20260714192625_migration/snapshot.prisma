datasource db {
  provider = "postgresql"
}

generator client {
  provider = "prisma-client"
  output   = "./generated"
  runtime  = "workerd"
}

model Widget {
  id   Int    @id @default(autoincrement())
  name String @unique

  @@map("alchemy_prisma_widgets")
}
