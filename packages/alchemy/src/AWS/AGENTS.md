# AWS service work

Before adding or completing an AWS service, load
`.agents/skills/alchemy-aws-service/SKILL.md` from the repository root.
The root `AGENTS.md` and its other task-specific skills still apply.
