# The demo terminal: one tmux window per tab. The tabs themselves are drawn
# as Ghostty's native tab bar by the Remotion terminal window.
set -g status off
set -g default-terminal "xterm-256color"
set -g mouse off
set -g escape-time 0
set -g history-limit 20000
set -g automatic-rename off
set -g allow-rename off
