{
  "workspaces": ["alchemy", "examples/*", "test"],
  "scripts": {
    "build": "tsc -b",
    "watch": "tsc -b --watch"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@types/bun": "latest",
    "@types/dotenv": "^8.2.3",
    "@types/node": "latest",
    "typescript": "latest"
  }
}
