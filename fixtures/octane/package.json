{
  "name": "@fixtures/octane",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run ../../packages/cloudflare-test-tools/e2e/src/Cli.ts dev --port 3112",
    "build": "bun run ../../packages/cloudflare-test-tools/e2e/src/Cli.ts build",
    "preview": "bun run ../../packages/cloudflare-test-tools/e2e/src/Cli.ts preview",
    "test": "playwright test",
    "pretest": "playwright install chromium && bun run build"
  },
  "dependencies": {
    "@octanejs/adapter-cloudflare": "0.0.12",
    "@octanejs/vite-plugin": "0.1.22",
    "octane": "0.1.22",
    "vite": "catalog:"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "catalog:workers",
    "@alchemy.run/cloudflare-test-tools": "workspace:*",
    "@alchemy.run/frameworks": "workspace:*",
    "@playwright/test": "catalog:",
    "@types/node": "^24.12.0"
  },
  "engines": {
    "node": ">=22"
  }
}
