---
layout: ../../../layouts/Marketing.astro
title: "Planetscale (coming soon) · alchemy"
description: "Planetscale support is on the way. Serverless MySQL with branching, declared in TypeScript, bound to your Workers and Lambdas."
---

import Section from "../../../components/marketing/Section.astro";
import Eyebrow from "../../../components/marketing/Eyebrow.astro";
import Button from "../../../components/marketing/Button.astro";
import CodeBlock from "../../../components/marketing/CodeBlock.astro";
import CTA from "../../../components/marketing/CTA.astro";
import PageHero from "../../../components/marketing/PageHero.astro";

export const PREVIEW_SRC = `// Sketch — coming soon
const db = yield* Planetscale.Database("Users", {
  region: "us-east",
  branch: "main",
});

export default class Api extends Cloudflare.Worker<Api>()(
  "Api",
  Effect.gen(function* () {
    const conn = yield* Planetscale.Connection.bind(db);
    return {
      fetch: () => conn.execute("SELECT * FROM users LIMIT 10"),
    };
  }),
) {}`;

<Section padding="60px 32px 36px">
  <PageHero
    eyebrow="provider · planetscale · coming soon"
    title="Planetscale, declared in TypeScript."
    highlight="Serverless MySQL."
    description="Branching databases, scaled to zero, bound to your Workers and Lambdas with the same .bind() shape as every other resource."
  >
    <Button slot="cta" variant="primary" icon="discord" href="#">Follow on Discord</Button>
    <Button slot="cta" variant="secondary" href="https://github.com/alchemy-run/alchemy">Watch on GitHub</Button>
  </PageHero>
</Section>

<Section padding="48px 32px 80px">
  <div style="max-width:760px;margin:0 auto 24px;text-align:center;">
    <p style="font-size:15.5px;line-height:1.65;color:var(--alc-fg-2);">
      Planetscale support is in development. The shape will mirror the rest of alchemy — declare a <code class="alc-code-inline" style="color:var(--alc-accent-deep);">Planetscale.Database</code> resource, bind it from any Worker or Lambda, get a typed connection.
    </p>
  </div>
  <div style="max-width:760px;margin:0 auto;">
    <CodeBlock filename="alchemy.run.ts (preview)" code={PREVIEW_SRC} />
  </div>

  <div style="max-width:920px;margin:36px auto 0;display:grid;grid-template-columns:1fr 1fr 1fr;gap:14px;">
    <div class="alc-card">
      <div class="alc-card__title">Branch per stage</div>
      <div class="alc-card__body">A Planetscale branch per stage — <code>dev_sam</code>, <code>pr-42</code>, <code>prod</code> — created and torn down with the rest of your stack.</div>
    </div>
    <div class="alc-card">
      <div class="alc-card__title">Typed schema</div>
      <div class="alc-card__body">Schema is declared in code. Migrations apply at deploy. Types flow into your queries.</div>
    </div>
    <div class="alc-card">
      <div class="alc-card__title">Same shape</div>
      <div class="alc-card__body">If you've used <code class="alc-code-inline" style="color:var(--alc-accent-deep);">.bind()</code> with R2 or DynamoDB, you've used Planetscale.</div>
    </div>
  </div>
</Section>

<CTA heading="Want this sooner? Tell us on Discord." />
