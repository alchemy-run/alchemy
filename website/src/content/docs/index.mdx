---
title: alchemy
description: Infrastructure-as-Effects. Build, test, and deploy cloud programs with type-safe infrastructure powered by Effect.
template: splash
hero:
  title: |
    Infrastructure as <span class="text-gradient">Effects</span>
  tagline: Your infrastructure and application logic in a single, type-safe program.
  actions:
    - text: Get Started
      link: /getting-started
      icon: right-arrow
      variant: primary
    - text: Tutorial
      link: /tutorial/part-1
      icon: open-book
      variant: secondary
---

import Terminal from "../../components/Terminal.astro";
import { LinkButton } from "@astrojs/starlight/components";

<div class="landing not-content">

{/* ── Hero code block ── */}

<section class="hero-code">
<div class="hero-code-grid">
<div class="hero-code-text">

<span class="section-eyebrow">One program · zero glue</span>

### Infrastructure and logic, together.

Alchemy extends Infrastructure-as-Code with **Effects** — your cloud resources and the code that uses them are defined together in a single type-safe program.

No YAML. No separate config files. No runtime wiring. <span class="alc-text-accent">If it compiles, it deploys.</span>

</div>
<div class="hero-code-block">

```typescript
// alchemy.run.ts
const Bucket = Cloudflare.R2Bucket("Bucket");

export default Alchemy.Stack(
  "MyApp",
  { providers: Cloudflare.providers() },
  Effect.gen(function* () {
    const bucket = yield* Bucket;
    const worker = yield* Worker;
    return { url: worker.url };
  }),
);
```

</div>
</div>
</section>

{/* ── Section break ── */}

<section class="section-intro">
<span class="section-eyebrow">What you get</span>
<h2 class="section-title">A type system that knows your cloud.</h2>
<p class="section-lede">Six properties of an alchemy stack. Each one is the language doing the work for you — not a tool you bolt on.</p>
</section>

{/* ── Feature grid ── */}

<section class="features">

<div class="feature">
<h3>Type-safe from cloud to code</h3>

The type system ensures every resource has its provider, every binding
is wired correctly, and every dependency is satisfied — before you
deploy.

```typescript title="alchemy.run.ts"
Alchemy.Stack(
  "App",
  {
    // Type Error: Layer<never> is not assignable
    // to Layer<Cloudflare.Providers>
    providers: Layer.empty,
  },
  Effect.gen(function* () {
    yield* Cloudflare.R2Bucket("Bucket");
  }),
);
```

</div>

<div class="feature">
<h3>Resources are just Effects</h3>

Resources are declared as Effects and composed with `yield*`. Import
them from any file, bind them to Workers, pass their outputs to other
resources — it's all just TypeScript.

```typescript
// src/bucket.ts
export const Bucket = Cloudflare.R2Bucket("Bucket");

// src/worker.ts
import { Bucket } from "./bucket.ts";

export default Cloudflare.Worker("Worker",
  { main: import.meta.path },
  Effect.gen(function* () {
    const bucket = yield* Cloudflare.R2Bucket.bind(Bucket);
    return { fetch: /* ... */ };
  }),
);
```

</div>

<div class="feature">
<h3>Test against real infrastructure</h3>

Deploy your stack in `beforeAll`, make HTTP requests against live
endpoints, and tear it down in `afterAll`. No mocks, no simulators —
real integration tests.

```typescript
// test/integ.test.ts
const stack = beforeAll(deploy(Stack));
afterAll.skipIf(!process.env.CI)(destroy(Stack));

test(
  "round-trip an object",
  Effect.gen(function* () {
    const { url } = yield* stack;
    yield* HttpClient.put(`${url}/hello.txt`, {
      body: HttpBody.text("Hello!"),
    });
    const res = yield* HttpClient.get(`${url}/hello.txt`);
    expect(yield* res.text).toBe("Hello!");
  }),
);
```

</div>

<div class="feature">
<h3>Local dev with hot reload</h3>

Run your entire stack locally with a single command. Workers run
locally in workerd, and changes hot reload instantly.

<Terminal content={`$ alchemy dev

[g]✓[/g] [b]Bucket[/b] [d](Cloudflare.R2Bucket)[/d] [g]created[/g] [d](local)[/d]
[g]✓[/g] [b]Worker[/b] [d](Cloudflare.Worker)[/d] [g]created[/g] [d](local)[/d]
[s2][d]• http://localhost:1337[/d]

[d]Watching for changes ...[/d]`} />

</div>

<div class="feature">
<h3>Plan, deploy, destroy</h3>

Preview what will change with `plan`, apply it with `deploy`, and
tear it down with `destroy`. Stages isolate environments so `dev`
and `prod` never collide.

<Terminal content={`$ alchemy deploy --stage prod

[u]Plan[/u]: [g]2 to create[/g]

[g]+[/g] [b]Bucket[/b] [d](Cloudflare.R2Bucket)[/d]
[g]+[/g] [b]Worker[/b] [d](Cloudflare.Worker)[/d] [c](1 bindings)[/c]
[s2][g]+[/g] [c]Bucket[/c]

Proceed?
◉ Yes ○ No
[g]✓[/g] [b]Bucket[/b] [d](Cloudflare.R2Bucket)[/d] [g]created[/g]
[g]✓[/g] [b]Worker[/b] [d](Cloudflare.Worker)[/d] [g]created[/g]`} />

</div>

<div class="feature">
<h3>CI/CD out of the box</h3>

Push to `main` to deploy prod. Open a PR and get an isolated preview
environment. Merge and it's cleaned up automatically. Alchemy even
posts the preview URL as a PR comment.

```yaml
# .github/workflows/deploy.yml
jobs:
  deploy:
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
      - run: bun install
      - run: bun alchemy deploy --stage ${{ env.STAGE }}
  cleanup:
    if: github.event.action == 'closed'
    steps:
      - run: bun alchemy destroy --stage ${{ env.STAGE }}
```

</div>

</section>

{/* ── Providers ── */}

<section class="providers-section">

<span class="section-eyebrow">Providers</span>
<h2 class="section-title">Deep on the clouds you actually ship on.</h2>
<p class="section-lede">Each provider is a typed Layer the compiler verifies at build time. Bring your own — every resource is just an Effect.</p>

<div class="provider-grid">
<div class="provider-card">

**Cloudflare**

Workers, R2 Buckets, KV, D1, Durable Objects, Queues, Vectorize, AI

</div>
<div class="provider-card">

**AWS**

Lambda, S3, DynamoDB, SQS, Kinesis, IAM, EC2, API Gateway

</div>
<div class="provider-card">

**More**

GitHub, Stripe, DNS, and a growing ecosystem of community providers

</div>
</div>

</section>

{/* ── Alternate framings (AB test) ── */}

<section class="providers-section explore-section">

<span class="section-eyebrow">A/B test · pick a lens</span>
<h2 class="section-title">Four ways to think about alchemy.</h2>
<p class="section-lede">Same product, different framings. Open the one that matches how you build.</p>

<div class="provider-grid">
<a class="provider-card" href="/effect">

**Effect**

Your cloud is one Effect. For engineers already deep in `Effect.gen`, `Context.Service`, and `Layer`.

</a>
<a class="provider-card" href="/iac">

**Infrastructure as Code**

Just TypeScript. Head-to-head against Pulumi, SST, Terraform, and CDK — speed, portability, no Go binary.

</a>
<a class="provider-card" href="/cloud">

**Cloud-first**

Deep on Cloudflare, AWS, and Planetscale. Typed resources, typed bindings, typed plans.

</a>
<a class="provider-card" href="/ai">

**AI / agents**

The shape your agent can ship. One TypeScript file from design to deploy — type system as guardrail.

</a>
</div>

</section>

{/* ── CTA ── */}

<section class="cta-section">

<span class="section-eyebrow">Get your hands dirty</span>
<h2 class="section-title">Zero to deployed Worker in 30 minutes.</h2>
<p class="section-lede">Follow the tutorial: a Cloudflare Worker with R2 bindings, real integration tests, local dev, and CI/CD — end to end.</p>

<div class="cta-buttons">
  <LinkButton href="/tutorial/part-1" variant="primary" icon="right-arrow">
    Start the Tutorial
  </LinkButton>
  <LinkButton href="/getting-started" variant="secondary" icon="open-book">
    Quick Start
  </LinkButton>
</div>

</section>

</div>
