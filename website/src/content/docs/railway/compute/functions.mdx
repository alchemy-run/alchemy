---
title: Functions, templates & VMs
description: Canvas Functions, marketplace templates, sandboxes, and cloud agents on Railway.
---

A [`Railway.Function`](/providers/railway/function) is a canvas
Function: a Service that runs a **single TypeScript file** on the
Bun function runtime. No GitHub repo. No Docker build. Distinct from
Effect-native [`Service({ main })`](/railway/compute/services).

Marketplace [`Template`](/providers/railway/template)s deploy a
serialized config into a Project. [`Sandbox`](/providers/railway/sandbox)
is an ephemeral Linux VM. [`CloudAgent`](/providers/railway/cloudagent)
is a coding-agent VM.

## HTTP Function

Pass inline `source` (or `path` to a `.ts` file). Alchemy generates
a `*.up.railway.app` domain unless `http: false` or a cron schedule
is set. Cap is 96KB once base64-encoded into the start command.

```typescript
const site = yield* Railway.Project("Site");
const ping = yield* Railway.Function("Ping", {
  project: site,
  source: `
    Bun.serve({
      hostname: "0.0.0.0",
      port: Number(process.env.PORT ?? 3000),
      fetch() { return new Response("ok"); },
    });
  `,
});
```

`path` is the CLI `--path` equivalent. The file is read at
plan/reconcile.

```typescript
const ping = yield* Railway.Function("Ping", {
  project: site,
  path: "./fn.ts",
});
```

## Cron Function

`cronSchedule` runs the Function on a cron expression (`--cron`).
HTTP domains are skipped unless `http: true` is set explicitly.

```typescript
const job = yield* Railway.Function("Cleanup", {
  project: site,
  source: `console.log("tick", new Date().toISOString());`,
  cronSchedule: "0 * * * *",
});
```

## Marketplace templates

`templateId` is a marketplace UUID or code (`postgres`). Pass a
Project to deploy into it, or omit `project` and Alchemy creates one.

```typescript
const site = yield* Railway.Project("Site");
const db = yield* Railway.Template("Postgres", {
  templateId: "postgres",
  project: site,
});
```

## Sandboxes

An ephemeral Linux VM. Create it, `execSandbox` commands, snapshot
with checkpoints, destroy it when the task is done. Sandboxes are
Priority Boarding.

```typescript
const box = yield* Railway.Sandbox("Box", {
  environment: site,
  idleTimeoutMinutes: 5,
});
yield* Railway.execSandbox({
  sandboxId: box.sandboxId,
  environmentId: box.environmentId,
  command: "echo hello",
});
```

## Cloud agents

A persistent coding-agent VM. Sleep keeps the disk; wake re-runs the
entrypoint. Variables are create-only and may use
[`Railway.ref`](/providers/railway/ref).

```typescript
const agent = yield* Railway.CloudAgent("Coder", {
  environment: site,
  variables: {
    DATABASE_URL: Railway.ref(db, "DATABASE_URL"),
  },
});
```

## Where next

[Services](/railway/compute/services) is the container for
Effect-native programs (`main` + `registry`).
The [`Function`](/providers/railway/function),
[`Template`](/providers/railway/template),
[`Sandbox`](/providers/railway/sandbox), and
[`CloudAgent`](/providers/railway/cloudagent) references list every
prop.
