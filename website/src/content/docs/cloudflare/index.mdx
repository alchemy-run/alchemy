---
title: Cloudflare
description: Build and deploy full applications on Cloudflare with Alchemy — one Worker runtime plus resources like Durable Objects, D1, R2, Queues, and Hyperdrive, wired together by typed bindings.
---

An alchemy app on Cloudflare is one **Worker** runtime plus the
resources it talks to — databases, object storage, queues,
stateful objects — all declared in the same TypeScript program and
wired together by typed bindings. Deploy the whole thing with
`bun alchemy deploy`; Alchemy figures out what changed.

New here? [Set up your account](/cloudflare/setup), then start the
[tutorial](/cloudflare/tutorial/part-1).

## Compute

- [Workers](/cloudflare/workers) — the compute runtime; every app has
  at least one.
- [Durable Objects](/cloudflare/durable-objects) — globally-unique
  stateful instances with transactional storage and typed RPC.
- [Containers](/cloudflare/containers) — long-lived processes and
  arbitrary runtimes, paired with a Durable Object for typed RPC.
- [Workflows](/cloudflare/workflows) — durable multi-step jobs with
  checkpointed, replayable steps.

## Data

- [D1](/cloudflare/d1) — serverless SQLite with migrations managed as
  part of the deploy.
- [KV](/cloudflare/kv) — edge key-value storage for config, sessions,
  and cached lookups.
- [R2](/cloudflare/r2) — object storage with read/write-scoped
  bindings.
- [Hyperdrive](/cloudflare/hyperdrive) — edge connection pooling for
  external Postgres and MySQL.

## Messaging

- [Queues](/cloudflare/queues) — at-least-once message delivery
  between Workers.

## Networking

- [Domains & DNS](/cloudflare/domains) — zones, DNS records, and zone
  settings as resources; adopt the domains you already own.

## Email

- [Email](/cloudflare/email) — route inbound mail on a zone you own
  and send from Workers.

## Security & secrets

- [Secrets & env](/cloudflare/secrets-env) — bind `.env` values and
  secrets into your Workers.

## What are you building?

| App shape | Stack |
| --- | --- |
| HTTP API | [Worker](/cloudflare/workers) + [D1](/cloudflare/d1) — see [Effect HTTP API](/cloudflare/guides/effect-http-api) |
| Typed RPC API | [Worker](/cloudflare/workers) — see [Effect RPC](/cloudflare/guides/effect-rpc) |
| Real-time / WebSockets | [Durable Objects](/cloudflare/durable-objects) — see [Accept WebSockets](/cloudflare/guides/hibernatable-websockets) |
| Full-stack app | [Worker](/cloudflare/workers) + Vite SPA — see [Add a Vite SPA](/cloudflare/guides/vite-spa) or [Frontend frameworks](/cloudflare/guides/frontends) |
| Background jobs | [Queues](/cloudflare/queues) |
| Postgres-backed API | [Hyperdrive](/cloudflare/hyperdrive) + [Drizzle](/cloudflare/guides/drizzle) |
| Durable multi-step jobs | [Workflows](/cloudflare/workflows) |
| AI apps | [AI Gateway](/cloudflare/guides/ai-gateway), [Effect AI](/cloudflare/guides/effect-ai), [AI Search](/cloudflare/guides/ai-search) |
| App on your own domain | [Domains & DNS](/cloudflare/domains) — see [Custom domains & routes](/cloudflare/guides/custom-domains) |
| Production observability | [Ship Worker telemetry to Axiom](/cloudflare/guides/axiom-observability) |

For frontends, alchemy is Workers-first: Pages resources exist, but
static assets served from a [Worker](/cloudflare/workers) is the
recommended path.

## Where next

- [Setup](/cloudflare/setup) — install alchemy and connect your
  Cloudflare account.
- [Tutorial](/cloudflare/tutorial/part-1) — from empty directory to a
  deployed Worker with tests, stages, and CI.
- [Custom domains & routes](/cloudflare/guides/custom-domains) — put
  your Worker on a domain you own.
- [Ship Worker telemetry to Axiom](/cloudflare/guides/axiom-observability) —
  datasets, ingest tokens, and monitors in the same Stack as the Worker.
- [API reference](/providers) — every Cloudflare resource and binding.

## Everything else

The blocks above are what most apps are built from, but coverage goes
much further: the zero-trust and zone-security long tail — roughly 60
more services, produced by the resource factory — ships as
reference-only documentation in the [API reference](/providers).
