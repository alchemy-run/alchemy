---
title: Docker
description: Images, containers, networks, and volumes as Stack resources, driven through your active Docker CLI context.
---

The Docker provider manages images, containers, networks, and volumes in the
same Stack as your cloud resources. Builds and local resources use the `docker`
CLI's active context — Docker Desktop, an SSH context, or a CI daemon.
Published-image reuse reads the registry directly without a builder.
`Cloudflare.Container` composes the same Docker image resources; immutable
image references separate image publication from application deployment.

New here? [Set up Docker](/docker/setup) first.

## Resources

`RemoteImage`, `Network`, `Volume`, and `Container` compose into a
local service — here, Postgres with a named volume, a network
alias, a published port, and a healthcheck:

```typescript
export default Alchemy.Stack(
  "DockerPostgresExample",
  {
    providers: Layer.merge(Docker.providers(), Alchemy.RandomProvider()),
    state: Alchemy.localState(),
  },
  Effect.gen(function* () {
    const password = yield* Alchemy.makeRandom("PostgresPassword", {
      bytes: 16,
    });

    const image = yield* Docker.RemoteImage("postgres-image", {
      source: "docker.io/library/postgres:18-alpine",
      alwaysPull: false,
    });
    const network = yield* Docker.Network("app-network");
    const data = yield* Docker.Volume("postgres-data");

    const postgres = yield* Docker.Container("postgres", {
      name: "alchemy-example-postgres",
      image,
      environment: {
        POSTGRES_DB: "app",
        POSTGRES_USER: "alchemy",
        POSTGRES_PASSWORD: password,
      },
      ports: [{ external: 15432, internal: 5432 }],
      volumes: [
        { hostPath: data.name, containerPath: "/var/lib/postgresql/data" },
      ],
      networks: [{ name: network.name, aliases: ["postgres"] }],
      healthcheck: {
        cmd: ["CMD-SHELL", "pg_isready -U alchemy -d app"],
        interval: "5 seconds",
        timeout: "5 seconds",
        retries: 10,
      },
      start: true,
    });
    return { container: postgres.name, hostPort: postgres.ports };
  }),
);
```

`Container` accepts the image resource directly, `alwaysPull: false`
pins the pulled tag so re-deploys are no-ops, and environment values
accept `Redacted` secrets — they are passed via process env, never
on the CLI. [Run local services](/docker/local-services) walks
through this composition.

`Image` builds from a Dockerfile:

```typescript
const image = yield* Docker.Image("app", {
  build: {
    context: "./app",
    dockerfile: "Dockerfile",
    args: { NODE_ENV: "production" },
  },
});
```

Planning hashes build inputs without building. Reconciliation reuses a matching
local image or published manifest, and builds only when needed. `image.ref`
returns a local image ID or a published digest reference. See
[Build and push an image](/docker/build-and-push) for publication, authentication,
cache settings, and explicit invalidation.

For a Cloudflare Container that owns its image, omit the image ID:

```typescript
const application = yield* Cloudflare.Container("Web", {
  image: Docker.Image({ context: "./app" }),
}).Application;
```

The no-ID form returns plain data. Cloudflare registers the real child image
resource; the helper itself performs no build. `Docker.RemoteImage({ source:
"nginx:alpine" })` provides the equivalent shorthand for an existing image.

`Docker.inspectContainer(name)` reads live runtime details for any
container by name, including the bound host ports.

:::caution
Most prop changes on `Network`, `Volume`, and `Container` are
**replacements** (delete-first) — the old resource is removed before
the new one is created. And a same-name pre-existing container,
network, or volume without alchemy labels surfaces as `Unowned`: the
deploy fails with `OwnedBySomeoneElse` until you opt in with
`.pipe(adopt(true))` or `--adopt`.
:::

## Compose with your cloud

[ECS](/aws/compute/ecs) Tasks build their image with your local
Docker — the AWS provider layers the same Docker service — and
[Cloudflare Containers](/cloudflare/compute/containers) bring your
own image by consuming the registry reference you push from
[Build and push an image](/docker/build-and-push). By contrast,
[MicroVMs](/aws/compute/microvms) build server-side on AWS and need
no local Docker at all.

## Reference

[Container](/providers/docker/container) ·
[Image](/providers/docker/image) ·
[Network](/providers/docker/network) ·
[RemoteImage](/providers/docker/remoteimage) ·
[Volume](/providers/docker/volume)
