---
title: 2.0.0-beta.77 - Profiles, CLI & Workers Tracing
date: 2026-09-08T09:00:00Z
category: release
excerpt: v2.0.0-beta.77 reworks auth profiles — stored as a folder per profile with a file per provider instead of a central profiles.json, managed from a redesigned alchemy profile TUI or from non-interactive subcommands built for agents — reorganizes the CLI commands, polishes the plan and dev output, and adds Cloudflare.Telemetry() for tracing Workers in Workers Observability. Also dependency-aware dev reloads, cron schedules on Workflows, provisioned ElastiCache, and private/pre-deploy/local-context Railway services.
---

beta.77 reworks how auth profiles are stored and managed. Profiles
are no longer a central `profiles.json` plus a separate credentials
tree; each profile is a folder under `~/.alchemy/profiles/` with
one file per connected provider (`cloudflare.json`, `aws.json`, …).
`alchemy login` is replaced by `alchemy profile`, a TUI dashboard
for adding, re-configuring, refreshing, and removing provider
accounts, and every action in it is also a non-interactive
subcommand so agents and scripts can do the same without a
terminal. The rest of the CLI is reorganized (`tail` →
`logs --follow`, `sync` → `drift --repair`, `aws` → `provider aws`,
…), the prompts are redesigned, and `alchemy dev` output is
polished: logs render above the plan/output widget, so you scroll
up for the full log history and press `←/→` to flip between the
stack's output and the most recently applied plan.
`Cloudflare.Telemetry()` sends Effect spans to Workers
Observability with no OTLP setup. Also: `alchemy dev` reloads only
when a file the stack depends on changes, Workflows take cron
schedules, and AWS gains provisioned ElastiCache.

<video controls muted playsinline preload="metadata" poster="/blog/beta-77/alchemy-cli-poster.png" src="/blog/beta-77/alchemy-cli.mp4" style="width: 100%; border-radius: 8px;"></video>

:::caution
**Breaking changes**

- **`alchemy deploy` defaults to `live_$USER`**
  ([#1421](https://github.com/alchemy-run/alchemy/pull/1421)) —
  `plan`, `destroy`, `logs`, and `drift` follow; `alchemy dev`
  keeps `dev_$USER`. A bare `alchemy deploy` will not update an
  existing `dev_sam` stack — pass `--stage dev_sam` to keep the
  old target. And the stage env var is now **`ALCHEMY_STAGE`**;
  `STAGE` is no longer consulted
  ([#1433](https://github.com/alchemy-run/alchemy/pull/1433)).
- **Commands moved**
  ([#1234](https://github.com/alchemy-run/alchemy/pull/1234),
  [#1235](https://github.com/alchemy-run/alchemy/pull/1235),
  [#1429](https://github.com/alchemy-run/alchemy/pull/1429)) —
  `alchemy login` is `alchemy profile`, the positional stack file
  is `--config`, `tail` is `logs --follow`, `sync` is
  `drift --repair`, `destroy --dry-run` is `plan --destroy`, and
  top-level `aws` / `cloudflare` are `provider aws` /
  `provider cloudflare`. `profile show|edit|refresh` take
  `--profile <name>` instead of a positional. The old names print
  a migration hint instead of running. See
  [below](#one-command-map).
- **The AWS `local` auth method is removed**
  ([#1480](https://github.com/alchemy-run/alchemy/pull/1480)) —
  `alchemy dev` wires the local emulator itself, so the profile
  method was redundant.
- **Custom auth providers** — `StackServices` lists `Interaction`
  instead of `CliKit`, and `configure`/`login`/`logout`/`details`
  require `Interaction` at call time
  ([#1430](https://github.com/alchemy-run/alchemy/pull/1430)). See
  [Custom Auth Providers](/environments/custom-auth-provider).
- **distilled operations are `verbNoun`**
  ([#1517](https://github.com/alchemy-run/alchemy/pull/1517)) —
  if you call a distilled client directly, `railway.serviceCreate`
  is now `railway.createService`, `volumeDelete` is
  `deleteVolume`, and so on. Alchemy's own resources are already
  renamed.
:::

## Profiles

A profile is a set of connected provider accounts — Cloudflare via
OAuth, AWS via SSO, GitHub via `gh`, a pasted token for Neon — that
every command authenticates with. That concept is unchanged. What
changed is how profiles are stored and how you manage them
([#1234](https://github.com/alchemy-run/alchemy/pull/1234),
[#1475](https://github.com/alchemy-run/alchemy/pull/1475),
[#1480](https://github.com/alchemy-run/alchemy/pull/1480)).

### Storage: a folder per profile, a file per provider

Before, alchemy kept a central `~/.alchemy/profiles.json` listing
every profile and which method each provider used, and a separate
`~/.alchemy/credentials/<profile>/` tree holding the secrets. Two
places had to agree, and a stale entry in one left the other
orphaned.

Now there is no central manifest. A profile is a directory, and
each connected provider is one JSON file in it holding its method,
configuration, and credentials together:

```text
~/.alchemy/profiles/
├─ default/
│  ├─ cloudflare.json   # method: oauth, accountId, tokens, scopes
│  └─ aws.json          # method: sso, ssoProfile
└─ work/
   └─ cloudflare.json   # method: stored, apiToken, accountId
```

Adding a provider writes a file; removing it deletes the file;
deleting a profile removes the directory. Directories are `0700`
and files `0600`. Each file is decoded against the provider's own
schema, so a corrupt or hand-edited file is backed up and reported
with a `--reconfigure` hint rather than half-loaded. Refreshes take
a cross-process lock, so two concurrent deploys cannot both rotate
the same OAuth token.

Existing installs migrate on first read: the old `profiles.json`
and `credentials/` move to `~/.alchemy/.profiles-v0-<timestamp>/`,
every profile and provider is rewritten in the new layout, and any
provider whose stored secret cannot be carried over reports
`NeedsReauth` with the command to fix it
([#1429](https://github.com/alchemy-run/alchemy/pull/1429)).

### Managing profiles interactively

Before, `alchemy login` ran a wizard — and `deploy` ran it for you
the first time it found a provider without credentials. The only
other profile commands were `show` and `clear`.

Now `alchemy profile` opens a dashboard. Profiles are tabs; each
connected provider is a row showing its method, credential status,
and details; and each action is a key:

```text
◆ default   work

❯ default · active

  Cloudflare  oauth  ✓ configured
    accountId  2b29022c…
    expires    in 59m
  AWS         sso    ✓ configured
    ssoProfile my-org

←/→ switch profile · ↑/↓ focus provider · e edit · r refresh · n new · q quit
```

`e` opens the edit menu, where Space cycles each provider through
add / re-configure / remove and Enter runs the queued flows in
order — OAuth opens the browser, SSO runs `aws sso login`, token
methods prompt for the fields. `r` refreshes credentials without
changing the method or account. `n` creates a profile, `R` renames
it (moving its directory), `d` deletes it.

### The same actions without a terminal

Every dashboard action is also a subcommand, and the subcommands
work without prompts:

```sh
alchemy profile list
alchemy profile show --profile work
alchemy profile create work
alchemy profile rename work staging
alchemy profile delete staging --yes

alchemy profile edit --add cloudflare              # runs the provider's flow
alchemy profile edit --reconfigure aws --profile work
alchemy profile edit --remove github
alchemy profile refresh --provider cloudflare
```

For agents and scripts, token-shaped methods are configured
entirely from flags. `--method` picks the provider's method and
`--set` supplies each field as a literal, an environment variable,
or stdin, so secrets never touch shell history:

```sh
alchemy profile edit --add cloudflare --method stored \
  --set apiToken=env:CLOUDFLARE_API_TOKEN \
  --set accountId=0123456789abcdef0123456789abcdef

op read op://vault/neon/key | alchemy profile edit --add neon --method stored --set apiKey=-
```

Without a TTY — CI, a pipe, a coding agent — bare `profile` prints
the subcommand help instead of the dashboard, `list` / `show` /
`current` print one plain line per entry, `edit` without action
flags prints its usage and exits non-zero, and every command exits
`0` / `1` / `130` so the caller can tell success from a declined
prompt from a cancellation.

### Who authenticates when

Only `profile` commands log in. `deploy`, `plan`, and `dev` never
open a wizard; a missing credential fails with `CredentialsRequired`
naming the resources that need it and the exact
`alchemy profile edit --add …` to run.

Outside CI, explicit provider environment variables
(`CLOUDFLARE_API_TOKEN`, `AWS_ACCESS_KEY_ID`, …) take precedence
over an implicit profile, decided per provider — Cloudflare from
env and AWS from the profile is fine — and the CLI logs which
variables it used, once
([#1520](https://github.com/alchemy-run/alchemy/pull/1520)). An
explicit `--profile` is always authoritative. In CI, profiles are
never read; `alchemy provider check-env` verifies the required
variables up front.

Docs: [profile](/cli/profile) · [Profiles](/environments/profiles)
· [Auth Providers](/environments/auth-providers) ·
[CI](/environments/ci).

## One command map

The CLI is reorganized around nouns
([#1235](https://github.com/alchemy-run/alchemy/pull/1235)):

```text
alchemy
├─ deploy | plan | destroy
├─ drift [--repair]
├─ dev
├─ logs [--follow]
├─ profile create|rename|edit|refresh|current|list|show|delete
├─ state list|read|delete
├─ unsafe nuke
└─ provider
   ├─ check-env
   ├─ aws bootstrap|teardown
   └─ cloudflare bootstrap|teardown|token|state logs
```

| Old                             | New                                  |
| ------------------------------- | ------------------------------------ |
| `alchemy login`                 | `alchemy profile`                    |
| `alchemy login --configure`     | `alchemy profile edit`               |
| `alchemy profile clear`         | `alchemy profile delete`             |
| `alchemy deploy ./stack.ts`     | `alchemy deploy --config ./stack.ts` |
| `alchemy tail`                  | `alchemy logs --follow`              |
| `alchemy sync`                  | `alchemy drift --repair`             |
| `alchemy destroy --dry-run`     | `alchemy plan --destroy`             |
| `alchemy aws` / `cloudflare`    | `alchemy provider aws` / `cloudflare` |

Exit codes are script-safe — `0` completed, `1` failure or a
declined plan, `130` cancelled — and every run writes a pruned
debug log with the full failure cause under `~/.alchemy/logs`.
Bare `profile` and `state` open their dashboards on a terminal and
print help everywhere else.

Docs: [CLI](/cli).

## Plan and dev output

The plan view is polished
([#1458](https://github.com/alchemy-run/alchemy/pull/1458),
[#1492](https://github.com/alchemy-run/alchemy/pull/1492)): totals
at the top, the plan in the middle — bounded to the terminal and
following the active row — and progress or controls at the bottom.
Binding rows no longer count toward apply progress, so a Worker
with changed bindings finishes at `n/n` instead of sitting at
`9/13`.

The bigger change is in `alchemy dev`. Logs render above the
plan/output widget in normal scrollback, so scrolling up shows the
full log history; the widget at the bottom shows either the
stack's output or the most recently applied plan. `←/→` flips
between them, `↑/↓` scrolls the widget, and `p` hides it — a
choice that survives the next hot reload.

Without a TTY — CI, a redirected pipe, or a coding agent
(`CLAUDECODE`, `CURSOR_AGENT`, `CODEX_CLI`, …) — the same commands
print append-only lines
([#1423](https://github.com/alchemy-run/alchemy/pull/1423),
[#1426](https://github.com/alchemy-run/alchemy/pull/1426)). Build
output and asset-upload progress stream as they happen instead of
being held until the resource settles, and long lines are written
whole instead of wrapping at column 80. Plain mode on a real
terminal still prompts for approval; `--no-input` disables prompts
and fails instead of hanging.

Slow shutdowns show progress
([#1427](https://github.com/alchemy-run/alchemy/pull/1427)) —
silent under 200ms, otherwise a spinner or periodic status line,
and a second Ctrl+C force-quits with exit `130`:

```text
^C
Shutting down — waiting for cleanup to finish (Ctrl+C again to force quit)
Still shutting down (5s)
```

Underneath, every surface is built on **CliKit**
([#1233](https://github.com/alchemy-run/alchemy/pull/1233)), a
terminal UI layer on
[`@alchemy.run/sigil`](https://npmx.dev/package/@alchemy.run/sigil),
our renderer fork — one root per output stream, serialized prompt
sessions, stdio capture that keeps logs ordered around live
regions, Unicode/ASCII fallbacks, and a lazily loaded runtime so
non-interactive commands never import React. Clack and the legacy
Ink renderer are gone. A later Sigil fix
([#1545](https://github.com/alchemy-run/alchemy/pull/1545)) stops
terminal capability replies from leaking into text inputs on slow
terminals and in Cursor.

## A redesigned TUI

Prompts and the profile dashboard drew nested vertical rails — a
rail on the frame *and* a rail on the focused row — so the focused
option read as `│ │ OAuth`
([#1547](https://github.com/alchemy-run/alchemy/pull/1547)). The
rails are gone. Focus and grouping are carried by glyphs and
indentation instead:

```diff
-› Cloudflare authentication method
-
-│ ❯ OAuth · recommended — browser-based login
-│   API Token or API Key · use an API token
+◆ Cloudflare authentication method
+  ❯ OAuth                 recommended — browser-based login
+    API Token or API Key  use an API token
```

`❯` is the one focus glyph everywhere — prompts, the edit-accounts
list, the dashboard's profile and provider rows — painted in the
brand color together with its label, so the pointer and the thing
it points at read as one block. The active prompt heading is `◆`,
so `❯` is the only chevron on screen; answered steps keep `✓`.
Labels and descriptions are aligned columns; searchable lists get
an fzf-style `/ query  3 of 12` row. The dashboard renders
providers as a table — `name  method  status` with `key  value`
details beneath — and virtualizes the list to the terminal height
so the tabs and header never scroll off
([#1533](https://github.com/alchemy-run/alchemy/pull/1533)).

## `Cloudflare.Telemetry()`

Effect spans from a Worker now land in
[Workers Observability](https://developers.cloudflare.com/workers/observability/)
([#1444](https://github.com/alchemy-run/alchemy/pull/1444)).
`Cloudflare.Telemetry()` is a Layer with the same shape as
`Axiom.Telemetry`: provide it on the Worker and your
`Effect.withSpan` frames appear in the Cloudflare dashboard, nested
under the platform's own spans.

```typescript
export default Cloudflare.Worker(
  "Api",
  { main: import.meta.url },
  Effect.gen(function* () {
    return {
      fetch: Effect.gen(function* () {
        const user = yield* kv.get("user:1").pipe(Effect.withSpan("load.user"));
        return yield* respond(user);
      }).pipe(Effect.withSpan("handle")),
    };
  }).pipe(Effect.provide(Cloudflare.Telemetry())),
);
```

```text
http.server GET
└─ handle
   ├─ load.user
   │  └─ kv_get
   └─ respond
```

Providing the Layer turns on `observability.traces` for the host
Worker at deploy time and installs a per-event Effect Tracer over
the runtime's `tracing.startActiveSpan`. Scalar span annotations
are forwarded as attributes; completion is recorded as
`effect.exit` (`success` / `failure` / `interrupted`). Cloudflare
owns sampling and export, so there is no OTLP endpoint and no
exporter to flush — `headSamplingRate` and `persist` pass straight
through to the Worker's observability config.

It composes with `Axiom.Telemetry`: provide both (and omit
`traces` on the Axiom side) to see the same waterfall in Axiom and
in Workers Observability. The Worker's compatibility date must be
`2026-07-28` or later; the deploy fails early otherwise.

Docs: [Native Workers tracing](/cloudflare/observability/workers-tracing).

## `alchemy dev` reloads

Dev reloads are owned by the exec child and are dependency-aware
([#1461](https://github.com/alchemy-run/alchemy/pull/1461),
[#1477](https://github.com/alchemy-run/alchemy/pull/1477)). The
watcher tracks the complete project-local import graph from your
entrypoint — sibling `infra/` and `src/` trees included — debounces
events, and compares content fingerprints, so a timestamp-only or
identical-content save does nothing. When a reload does fire, the
CLI logs which files' changed contents caused it. One Node process
stays alive and imports each stack generation under a fresh module
namespace; Bun respawns only the exec child and keeps the sidecar.

Node runs TypeScript through Alchemy's Oxc loader
(`@alchemy.run/node-utils/register-oxc`) instead of Node's
strip-only support: TSX, tsconfig `paths`, extensionless imports,
JSON imports, and inline source maps all work. Node `22.15+` /
`23.5+` / `24+` is required for the `module.registerHooks` it
relies on.

`alchemy dev` runs without credentials it does not use
([#1480](https://github.com/alchemy-run/alchemy/pull/1480),
[#1430](https://github.com/alchemy-run/alchemy/pull/1430)): the
AWS environment resolves lazily, and only `Alchemy.remote()` rows
ask for a profile at plan time.

Docs: [dev](/cli/dev).

## Workflows: native cron schedules

Cloudflare Workflows take wrangler-compatible `schedules`, so a
cron expression creates instances natively — no Worker Cron
Trigger calling `workflow.create()`
([#1491](https://github.com/alchemy-run/alchemy/pull/1491)):

```typescript
export default class Hourly extends Cloudflare.Workflow<Hourly>()(
  "Hourly",
  { schedules: ["0 * * * *"] },
  Effect.gen(function* () {
    return Effect.fn(function* () {
      const event = yield* Cloudflare.Workflows.WorkflowEvent;
      return event.schedule?.cron;
    });
  }),
) {}
```

Async Workers take the same array on the binding; `schedules: []`
clears.

## Provisioned ElastiCache

`AWS.ElastiCache` adds `SubnetGroup`, `ReplicationGroup` (Valkey
and Redis OSS, with replicas, Multi-AZ, shard and tag updates in
place), and `CacheCluster` (Memcached) alongside the existing
serverless cache
([#1422](https://github.com/alchemy-run/alchemy/pull/1422)).
`ConnectReplicationGroup` / `ConnectCacheCluster` publish the
endpoint to a Lambda and can attach it to the cache's subnets and
security groups in the same call. Thanks
[Henning Pokriefke](https://github.com/henningpokriefke)!

```typescript
const cache = yield* AWS.ElastiCache.ReplicationGroup("Cache", {
  engine: "valkey",
  nodeType: "cache.t4g.micro",
  subnetGroupName: subnetGroup.subnetGroupName,
  securityGroupIds: [sg.groupId],
  replicasPerNodeGroup: 1,
  automaticFailoverEnabled: true,
  multiAzEnabled: true,
});

// inside the Lambda: endpoint env + VPC attachment
const conn = yield* AWS.ElastiCache.ConnectReplicationGroup(cache, {
  subnetIds: vpc.privateSubnetIds,
  securityGroupIds: [sg.groupId],
});
```

## Railway: private services, pre-deploy, local contexts

Three additions to `Railway.Service`, all from
[Dallen Pyrah](https://github.com/dallenpyrah) — thanks!

- **`publicDomain: false`**
  ([#1439](https://github.com/alchemy-run/alchemy/pull/1439)) —
  private-mesh only (`{name}.railway.internal`, no `url`).
- **`preDeploy: { command }`**
  ([#1438](https://github.com/alchemy-run/alchemy/pull/1438)) —
  a one-shot job between image build and process start, for
  migrations and seeds. `{ command: null }` clears.
- **`context: "./api"`**
  ([#1440](https://github.com/alchemy-run/alchemy/pull/1440)) —
  deploy a local Docker context through Railway's upload path,
  with Docker ignore semantics and a content hash as the
  deployment identity.

```typescript
const worker = yield* Railway.Service("Worker", {
  project,
  context: "./worker",
  preDeploy: { command: "bun migrate" },
  publicDomain: false,
});
```

Railway CLI-login tokens also resolve their workspace again
([#1479](https://github.com/alchemy-run/alchemy/pull/1479)) — the
generated `me` query no longer selects an admin-only field.

## Also in this release

- **Fly health checks**
  ([#1463](https://github.com/alchemy-run/alchemy/pull/1463)) —
  `services[].checks` on `Fly.Machine` and `Fly.Service`. Thanks
  [Adam Svystun](https://github.com/adamsvystun)! Fly Machines
  requests also stop hitting `/v1/v1/…`
  ([#1543](https://github.com/alchemy-run/alchemy/pull/1543)).
- **Durable Objects: `webSocketError` and caller context**
  ([#1497](https://github.com/alchemy-run/alchemy/pull/1497),
  [#1498](https://github.com/alchemy-run/alchemy/pull/1498)) — the
  Effect shape gains `webSocketError(socket, error)`, and
  `blockConcurrencyWhile` / `storage.transaction` run their
  closures with the calling fiber's context and carry its `R`.
  Thanks [Alex](https://github.com/agcty)!
  `state.abort(reason, { retryAlarm: false })` stops an
  interrupted alarm from retrying
  ([#1374](https://github.com/alchemy-run/alchemy/pull/1374)).
- **`Cloudflare.Worker.ref` typechecks**
  ([#1434](https://github.com/alchemy-run/alchemy/pull/1434)) —
  the documented cross-stage Worker reference was missing from the
  type. Thanks [pollux](https://github.com/hello-pollux)!
- **Worker Loader `get()` returns a usable stub**
  ([#1488](https://github.com/alchemy-run/alchemy/pull/1488)) and
  **`RpcWorker` keeps its logical ID**
  ([#1487](https://github.com/alchemy-run/alchemy/pull/1487)).
- **Asset uploads run three buckets at a time**
  ([#1445](https://github.com/alchemy-run/alchemy/pull/1445)) —
  matching wrangler, dropping to one on the first gateway error.
  Thanks [Rahul Mishra](https://github.com/BlankParticle), who
  also bumped workerd and the vendored Workers runtime to
  `20260901.1`
  ([#1442](https://github.com/alchemy-run/alchemy/pull/1442))!
  Internal isolates pin compatibility date `2026-08-31`
  ([#1380](https://github.com/alchemy-run/alchemy/pull/1380)).
- **Workflow steps with `retries` but no `timeout` no longer
  crash**
  ([#1416](https://github.com/alchemy-run/alchemy/pull/1416)) —
  thanks [Lord Coughmann](https://github.com/LordCoughmann)!
- **`_headers` / `_redirects` no longer crash local dev**
  ([#1418](https://github.com/alchemy-run/alchemy/pull/1418)) —
  thanks [Michael](https://github.com/deracs)!
- **`nuke` progress restored**
  ([#1532](https://github.com/alchemy-run/alchemy/pull/1532)) —
  scan progress, per-resource identity and deletion status in the
  shared plan view, and `alchemy state unsafe nuke` for bulk state
  cleanup.
- **`provider cloudflare bootstrap` honors `--profile`**
  ([#1538](https://github.com/alchemy-run/alchemy/pull/1538)).
- **PR preview packages live as long as the PR**
  ([#1511](https://github.com/alchemy-run/alchemy/pull/1511)) —
  thanks [Michael K](https://github.com/Mkassabov), who also
  bumped distilled to the `verbNoun` generation
  ([#1517](https://github.com/alchemy-run/alchemy/pull/1517)) and
  pinned CORS on plain Worker fetch
  ([#1541](https://github.com/alchemy-run/alchemy/pull/1541))!
- **Docs rewritten as a narrative**
  ([#1470](https://github.com/alchemy-run/alchemy/pull/1470),
  [#1472](https://github.com/alchemy-run/alchemy/pull/1472),
  [#1424](https://github.com/alchemy-run/alchemy/pull/1424)) —
  "Runtime" replaces "Functions & Servers", the two phases are the
  Construction phase and the Runtime phase, and Getting started,
  What is Alchemy, and the Workers hub read top to bottom.

## Where to go next

- [CLI](/cli)
- [profile](/cli/profile)
- [Profiles](/environments/profiles)
- [Auth Providers](/environments/auth-providers)
- [CI](/environments/ci)
- [dev](/cli/dev)
- [Native Workers tracing](/cloudflare/observability/workers-tracing)
- [Stages](/environments/stages)
- [CHANGELOG](https://github.com/alchemy-run/alchemy/blob/main/CHANGELOG.md#v200-beta77)
- [Compare v2.0.0-beta.76 → v2.0.0-beta.77](https://github.com/alchemy-run/alchemy/compare/v2.0.0-beta.76...v2.0.0-beta.77)
