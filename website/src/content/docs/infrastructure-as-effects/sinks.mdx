---
title: Sinks
description: A Sink is the write-side dual of an Event Source. It is a Binding that exposes a resource as an Effect Sink, batching writes into the batch API with least-privilege IAM.
---

A **Sink** is the dual of an [Event Source](/infrastructure-as-effects/event-sources).
It is a [Binding](/infrastructure-as-effects/binding) for *writing* that
exposes a resource as an Effect
[`Sink`](https://effect.website/docs/sink/introduction/). An
Event Source hands you records as a `Stream`. A Sink accepts a
`Stream` and drains it into the resource's batch API
(`SendMessageBatch`, `PublishBatch`, `PutRecords`). It also emits the
minimal IAM to go with it.

## A Sink in a constructor

Like every Binding, a Sink is declared in the
[Effectful Constructor](/infrastructure-as-effects/runtime#the-effectful-constructor)
and used in the interface it returns:

```typescript
import * as AWS from "alchemy/AWS";
import * as Effect from "effect/Effect";
import * as Layer from "effect/Layer";
import * as Stream from "effect/Stream";
import { HttpServerRequest } from "effect/unstable/http/HttpServerRequest";
import * as HttpServerResponse from "effect/unstable/http/HttpServerResponse";
import { OutboundQueue } from "./queue.ts";

export default class Publisher extends AWS.Lambda.Function<Publisher>()(
  "Publisher",
  { main: import.meta.url, functionUrl: true },
  Effect.gen(function* () {
    const sink = yield* AWS.SQS.QueueSink(OutboundQueue);
    // → Sink<void, QueueSinkEntry, readonly QueueSinkEntry[], QueueSinkError>
    // → policy: sqs:SendMessage + sqs:SendMessageBatch on OutboundQueue's ARN

    return {
      fetch: Effect.gen(function* () {
        const request = yield* HttpServerRequest;
        const body = (yield* request.json) as { messages: string[] };

        yield* Stream.fromIterable(body.messages).pipe(
          Stream.map((message) => ({ MessageBody: message })),
          Stream.run(sink),
        );

        return yield* HttpServerResponse.json({
          ok: true,
          count: body.messages.length,
        });
      }),
    };
  }).pipe(
    Effect.provide(
      AWS.SQS.QueueSinkHttp.pipe(
        Layer.provideMerge(AWS.SQS.SendMessageBatchHttp),
      ),
    ),
  ),
) {}
```

`QueueSink` is the contract. The provided `AWS.SQS.QueueSinkHttp`
Layer implements it over `SendMessageBatch`, which is itself a
contract satisfied by `SendMessageBatchHttp`. Implementation Layers
can depend on other contracts, so the provide chains a
`Layer.provideMerge`. The Layer attaches the least-privilege policy
statements to the Function's role. The `yield*` returns a plain
Effect `Sink`. You don't read a `QUEUE_URL` env var, and you don't
chunk `SendMessageBatchCommand` calls by hand. Every Binding follows
the same contract-and-Layer split. See
[Bindings › A contract and a Layer](/infrastructure-as-effects/binding#a-contract-and-a-layer).

## Element types

Sinks are typed by the element they accept, so the type system stops
you from feeding the wrong shape in:

| Sink                          | Element                  | Batch API          | IAM emitted                                    |
| ----------------------------- | ------------------------ | ------------------ | ---------------------------------------------- |
| `SQS.QueueSink(queue)`        | `QueueSinkEntry`         | `SendMessageBatch` | `sqs:SendMessage`, `sqs:SendMessageBatch`      |
| `SNS.TopicSink(topic)`        | `TopicSinkEntry`         | `PublishBatch`     | `sns:Publish`                                  |
| `Kinesis.StreamSink(stream)`  | `PutRecordsRequestEntry` | `PutRecords`       | `kinesis:PutRecords`                           |

Every policy is scoped to the exact resource ARN.

`QueueSink` takes a `QueueSinkEntry`, which is an SQS `SendMessageBatchRequestEntry`
without its `Id`, so you set `MessageBody`, `MessageGroupId`, message
attributes, and so on. `TopicSink` takes a `TopicSinkEntry`, which is an SNS
`PublishBatchRequestEntry` without its `Id`, so you set `Message`,
`Subject`, and so on. The sink assigns each batch entry's `Id` itself.
`StreamSink` takes raw `PutRecordsRequestEntry` values, so you keep
control of `PartitionKey`:

```typescript
export default class Ingest extends AWS.Lambda.Function<Ingest>()(
  "Ingest",
  { main: import.meta.url, functionUrl: true },
  Effect.gen(function* () {
    const sink = yield* AWS.Kinesis.StreamSink(ClickStream);

    return {
      fetch: Effect.gen(function* () {
        const request = yield* HttpServerRequest;
        const body = (yield* request.json) as {
          records: Array<{ partitionKey: string; data: string }>;
        };

        yield* Stream.fromIterable(
          body.records.map((record) => ({
            PartitionKey: record.partitionKey,
            Data: new TextEncoder().encode(record.data),
          })),
        ).pipe(Stream.run(sink));

        return yield* HttpServerResponse.json({ ok: true });
      }),
    };
  }),
  // (layers elided — same shape as Publisher above:
  //  AWS.Kinesis.StreamSinkHttp over AWS.Kinesis.PutRecordsHttp)
) {}
```

## Batching

A Sink drains its input chunk by chunk. It splits each chunk into
batches that fit the API's limits, keeping the input order:

| Sink                 | Max entries per call | Max bytes per call |
| -------------------- | -------------------- | ------------------ |
| `SQS.QueueSink`      | 10                   | 256 KiB            |
| `SNS.TopicSink`      | 10                   | 256 KiB            |
| `Kinesis.StreamSink` | 500                  | 5 MiB              |

The sink sends the batches one at a time, in order. After each call it
checks the per-entry results:

- Transient failures are resent. For SQS and SNS these are entries
  with `SenderFault: false`, such as throttling or internal errors.
  Kinesis treats every failed record (throughput exceeded or internal
  failure) as transient.
- Permanent rejections are dropped with a warning log. Only
  `SQS.QueueSink` and `SNS.TopicSink` drop entries, and only those that
  come back with `SenderFault: true`.
- The sink resends up to five times, with exponential backoff starting
  at 200 ms. If entries are still failing after that, it fails with a
  `BatchRetryExhaustedError` whose `entries` field holds them.

A failed API call also fails the sink, with the batch API's own error
(e.g. `SendMessageBatchError`).

The sink never makes a batch larger than the chunk it receives. To send
smaller batches, rechunk the stream before running it into the sink:

```typescript
yield* Stream.fromIterable(messages).pipe(
  Stream.map((message) => ({ MessageBody: message })),
  Stream.rechunk(5), // at most 5 messages per SendMessageBatch call
  Stream.run(sink),
);
```

Inside an event-source handler, whose error type is `never`, end the
pipeline with `Effect.orDie`, or handle every error the sink can fail
with. Catch `BatchRetryExhaustedError` with `Effect.catchTag` to decide
what happens to the stranded entries.

## Source → transform → sink

The model pays off when you compose a Sink with an
[Event Source](/infrastructure-as-effects/event-sources). Every step
is a `Stream` or a `Sink`, so the whole pipe-and-transform pipeline
is one expression inside the constructor:

```typescript
type OrderImage = { id: { S: string }; status: { S: string } };

export default class OrderRelay extends AWS.Lambda.Function<OrderRelay>()(
  "OrderRelay",
  { main: import.meta.url },
  Effect.gen(function* () {
    const sink = yield* AWS.SQS.QueueSink(OutboundQueue);

    yield* AWS.DynamoDB.consumeTableChanges<OrderImage>(
      OrdersTable,
      {
        streamViewType: "NEW_AND_OLD_IMAGES",
        startingPosition: "LATEST",
      },
      (records) =>
        records.pipe(
          Stream.map((r) => r.dynamodb.NewImage),
          Stream.filter(
            (image): image is OrderImage => image?.status.S === "PAID",
          ),
          Stream.map((image) => ({
            MessageBody: JSON.stringify({ orderId: image.id.S }),
          })),
          Stream.run(sink),
          Effect.orDie,
        ),
    );
  }).pipe(
    Effect.provide(
      Layer.mergeAll(AWS.Lambda.TableEventSource, AWS.SQS.QueueSinkHttp).pipe(
        Layer.provideMerge(AWS.SQS.SendMessageBatchHttp),
      ),
    ),
  ),
) {}
```

Stream records arrive as DynamoDB attribute-value maps. Alchemy does
not unmarshall them, so `OrderImage` describes the raw shape
(`{ S: string }` per string attribute) and the filter reads
`status.S`. The type guard on `Stream.filter` also drops records with
no `NewImage`, such as deletes. `Effect.orDie` turns the sink's error
into a defect, because the handler's error type is `never`.

The pipeline uses two binding calls. The provided Layers attach both
sides of the policy to the Function's role. These are
`dynamodb:DescribeStream`, `GetRecords`, and `GetShardIterator` on
`OrdersTable`'s stream, plus `sqs:SendMessage` and `SendMessageBatch`
on `OutboundQueue`. `AWS.Lambda.TableEventSource` wires the
event-source mapping.

You can add `Effect.retry`, `Stream.throttle`, `Stream.groupedWithin`,
or `Stream.mapEffect` anywhere along the chain. They are all the
same `Stream` code you'd write in a plain Effect program.

## Where next

- [Event Sources](/infrastructure-as-effects/event-sources): the read-side dual, which exposes resources as `Stream`s.
- [Bindings](/infrastructure-as-effects/binding): the deploy-time mechanics every Sink shares.
- [Layers](/infrastructure-as-effects/layers): hide Sinks behind a service interface.
